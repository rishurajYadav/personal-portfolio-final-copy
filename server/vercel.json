{
    "builds": [
        {
        "src": "./server.js",
        "use": "@vercel/node"
        }

    ],
    "routes": [
        {
            "src": "/(.*)",
            "dist": "server.js"
        }
    ]
}